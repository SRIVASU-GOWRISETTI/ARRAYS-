

char* reverse(char *S, int len)
{
    //code here
    stack<int> ans;
    for(int i=0;i<len;i++){
        
        ans.push(S[i]);
        
    }
    int i=0;
    while(!ans.empty()){
        
        S[i]=ans.top();
        ans.pop();
        i++;
        }
        return S;
}
